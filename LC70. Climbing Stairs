class Solution {
    public int climbStairs(int n) {
        if(n <= 1) return n;
        int m1 = 1, m2 = 1;
        for(int i=2; i<=n; i++) {
            int tmp = m1 + m2;
            m1 = m2;
            m2 = tmp;
        }
        return m2;
    }
}

// time: O(n)
// space: O(1)
